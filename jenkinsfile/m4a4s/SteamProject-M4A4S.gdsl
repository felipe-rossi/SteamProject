pipeline{
    agent any
    stages{
        stage('Iniciar Testes'){
            steps{
                bat "mvn test -Dtest=T005M4A4STest"
            }
        }
    }
    post{
        always{
            junit allowEmptyResults: true, testResults: '/target/surefire-reports/*.xml'
        }
    }
}